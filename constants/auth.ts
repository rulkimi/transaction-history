/**
 * The duration of inactivity (in minutes) after which the user
 * must re-authenticate with biometrics.
 */
export const AUTH_EXPIRY_MINUTES = 5;
export const AUTH_EXPIRY_MS = AUTH_EXPIRY_MINUTES * 60 * 1000;
